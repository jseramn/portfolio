# Review target — U07 lazy Motion (PR from `swarm/hero-lazy-motion`)

WORKTREE: `/home/jseramn/portfolio-worktrees/swarm/hero-lazy-motion` @ `9dff534`.
PR: https://github.com/jseramn/portfolio/pull/47 — `perf(hero): hydrate Motion-driven chrome after first paint`.
**Base is `main`.** `git merge-base --is-ancestor origin/main HEAD` is 0. Review `origin/main...HEAD` (+287/−49, 11 files), not integration.
Commits on main: `d7e4380` lazy Motion; `59c619d` restore Tinity CTA; `9dff534` drop duplicate Layout tinity prop after rebase.
Unit brief: `/tmp/swarm/prompts/U07-lazy-motion.md`. PR body: `/tmp/swarm/pr/hero-lazy-motion.md`.
Worker contract: `/tmp/swarm/prompts/_worker_common.md`. Cite `design.md` §7 and B-09.
Wave 3 prod-r1 (without this PR): `/tmp/swarm/measure/prod-r1/report.md` — mobile P90, TBT 390 ms, Hero chunk still ~58 KB with motion.

You are READ-ONLY. Do not edit. Write `/tmp/swarm/reviews/U07.md`, then `gh pr comment 47 --body-file /tmp/swarm/reviews/U07.md`. Print the same report. Same-account cannot `gh pr review --approve`.

## Focus points
1. **Bundle** — `Hero.*.js` island entry must not contain `motion`. Report before/after sizes. `TextLoop` / `InfiniteSlider` hydrate via `lazy()` + `Suspense` after first paint (idle + 2 s fallback, or boot-ready). SSR first-role + marquee markup must match the hydrated initial DOM (CLS 0).
2. **U22 preserved** — `useReducedMotion` freeze, `phase: "static"` fallback, `glow-pulse` class (not inline animation) still work. Do not bounce if reduced-motion stays static before Motion hydrates.
3. **U26 preserved** — music failure / ramp / now-playing. Tinity CTA (`Open Tinity`, `site.tinity.path`) next to socials, hire unchanged. Home glass: Tinity adds a wrap (5 original hero + Tinity + modal). No `--hero-ink*` / scrim retint. Tagline stays SSR LCP node. Layout keeps `tinity?: boolean` from #46 and U10 CSS (inline globals on home only).
4. **Tests** — Vitest `renderToStaticMarkup` equality; Playwright marquee/roles after load (poll ≤5 s); CLS `layout-shift` sum < 0.01. No `networkidle` on `/`.
5. Size: unit target 250; authored **336** vs main because of Tinity CTA restore — under the 400 PR budget, accept with a note, not a blocker. Gates: tsc, vitest, astro check, flock e2e + build. Unique `PLAYWRIGHT_PORT`. `AGENT_BASE_URL=http://127.0.0.1:1` if :4321 is foreign. Preview SSO: one GET, do not bounce. LH on preview may 302 — local/build numbers are enough.

APPROVE if motion leaves the critical chunk, CLS stays 0, U22/U26/Tinity-CTA survive, Layout tinity prop stays, and gates pass.
