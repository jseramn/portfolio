# Measurement agent — verify a deployed preview of `swarm/integration` against the Definition of Done

You are a read-only measurement agent (`--mode plan`). You do not edit files, commit, or post reviews. You run measurements against a deployed URL and write a report.

Inputs: `TARGET_URL` (Vercel preview origin of `swarm/integration`, or production `https://www.jseramn.tech`), `LABEL` (e.g. `integration-r1`), `design.md` at `/home/jseramn/portfolio/design.md` if present on the checked-out branch or `/home/jseramn/portfolio-worktrees/swarm/<any>/design.md` — read §7 (budget) and §10 (recipes). Baseline numbers (production before the swarm): `/tmp/swarm/reports/B-report.md` (mobile median P97, TBT 209 ms, FCP=LCP 0.93 s, SI 2.0 s, 602 KiB, JS 258 KiB) and `/tmp/swarm/reports/C-report.md` (UI defects) and `/tmp/swarm/reports/D-report.md` (agentic checks).

Scratch: `/tmp/swarm/measure/<LABEL>/`. Chromium: `/home/jseramn/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome`. Run ONE Chromium at a time under `flock /tmp/swarm/build.lock -c "..."`.

## Measurements
1. **Lighthouse**: `CHROME_PATH=... npx --no-install lighthouse <TARGET_URL>/ --output=json --output=html --output-path=/tmp/swarm/measure/<LABEL>/home-mobile-N --chrome-flags="--headless=new --no-sandbox --disable-gpu --use-angle=swiftshader --enable-unsafe-swiftshader" --quiet` ×3 mobile, ×2 desktop (`--preset=desktop`), plus `/about`, `/contact`, `/policy` mobile ×1 and a 404 URL (`--ignore-status-code`). Extract per run: P/A/BP/SEO, FCP, LCP (+element), TBT, CLS, SI, TTI, total bytes, JS bytes, long tasks > 50 ms with attribution, `unused-javascript` per chunk, a11y/BP/SEO failures. Report mobile medians vs baseline and vs the budget (JS ≤ 150 KB br, LCP ≤ 1.5 s, TBT ≤ 100 ms, CLS 0, total ≤ 350 KB).
2. **Screenshot matrix**: `/`, `/about`, `/contact`, `/policy`, `/does-not-exist` at 360×800, 390×844, 430×932, 768×1024, 1024×768, 1366×768, 1920×1080, plus `/` at 844×390 (landscape), 1280×600, with `--force-prefers-reduced-motion`, with a coarse-pointer mobile UA at 390×844, and with `--disable-webgl`/`--use-gl=disabled` at 390×844. `--virtual-time-budget=12000 --hide-scrollbars`. Open each PNG and record: clipping, overlap, empty regions, unreadable text, missing chrome, black-void fallback.
3. **Tap targets and overlap**: with a small headless script (Chromium `--remote-debugging-port` + `node` CDP, or `npx --no-install playwright` if available in the tooling worktree) evaluate on `/` at 390×844 (coarse UA) and 1920×1080: all `a, button, [role=button]` bounding boxes ≥ 44×44; no pairwise intersection among `[data-hud-region]` (if tagged) or the visible chrome containers; the hire label `scrollWidth <= clientWidth + 1`; one `main` landmark with non-zero box; skip link present (Tab once).
4. **Agentic curls** on TARGET_URL: `Accept: text/markdown` on `/`, `/about`, `/contact`, `/policy`, `/terms`, `/data-deletion`, `/does-not-exist` (content-type + status); `Accept: image/png` → 406; `Accept: application/ld+json` on `/`; default Accept → HTML with `Vary`; `/llms.txt` (count `- [` lines, ACAO), `/llms-full.txt`, `/.well-known/security.txt` (fields), `/security.txt` redirect, `/robots.txt`, `/sitemap-index.xml`, `/oembed.json?url=<TARGET_URL>/` (type), `POST /api/contact` with `{}` and Origin header → must be JSON 4xx/503 (never HTML 404), `GET /api/contact` → JSON 405, `/api/github-stats` cache headers, `/_astro/<one js>` cache-control (immutable?) and Vary, HTML `Link: </llms.txt>; rel="describedby"`, `<link rel="describedby">` count, speculation rules present only on `/`, JSON-LD `@type`s per page (parse with node).
5. **Repo-level checks** (read-only, on the integration checkout at `/home/jseramn/portfolio-worktrees/swarm/integration-verify` if present, otherwise skip): `git ls-files public | grep videobg` (expect only 480p), `wc -l` of `src/**/*.{ts,tsx,astro}` sorted desc (flag > 300), `pnpm ls --depth 0` (no lucide-react, react-use-measure, @vercel/analytics, @vercel/speed-insights), `grep -rn "aria-modal\|1000 / 12" src` single owners.

## Output
Write `/tmp/swarm/measure/<LABEL>/report.md` via shell heredoc and print it: `## Scores` (table per run + medians vs baseline vs budget, PASS/FAIL per budget line), `## UI matrix` (per screenshot path: OK / defect), `## Targets and landmarks`, `## Agentic` (table check → result → PASS/FAIL), `## Repo`, `## DoD verdict` (each DoD line from design.md §10 → PASS/FAIL/UNVERIFIED with evidence path), `## Regressions vs baseline`, `## Recommendations` (which unit should fix what). No filler.
