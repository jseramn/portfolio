# Review target — U14 Hero split + glass pump

WORKTREE: `/home/jseramn/portfolio-worktrees/swarm/hero-split`.
Find the PR from `swarm/hero-split` against **`main`**. Unit brief: `/tmp/swarm/prompts/U14-hero-split.md`.
Worker contract: `/tmp/swarm/prompts/_worker_common.md`. Cite `design.md` §3/§6.

You are READ-ONLY. Do not edit. Write `/tmp/swarm/reviews/U14.md`, then `gh pr comment <N> --body-file /tmp/swarm/reviews/U14.md`. Same-account cannot `gh pr review --approve`.

## Focus
1. **Move only** — className, DOM order, `data-*`, aria identical. SSR `/` diff empty (hashed ids normalised). No `--hero-ink*` / scrim / type restyle.
2. **U07 survives** — no static `motion/react` in the critical island; `scheduleHeroMotionChrome` / `heroMotionStatic` still gate TextLoop/InfiniteSlider.
3. **Tinity CTA** — `Open Tinity` + `site.tinity.path` still in chrome (likely `HeroSocials`); hire label unchanged.
4. **Pump** — `src/lib/glass/pump.ts` owns rAF singleton; register/unregister + tests with fake rAF; GlassSurface has no module-level `glassJobs` leftover; cleanup on unmount.
5. **LOC** — `Hero.tsx` ≤150; other `src/components` product files ≤300 except pre-existing `ContactModal.tsx`. Count `<GlassSurface` across new files: 6 hero + 1 modal.
6. `size:exception` required (moves). Gates: tsc, vitest, astro check, flock build + e2e. No `networkidle` on `/`.

APPROVE only if evidence shows zero visual/behaviour change and U07/U22/U26/Tinity still hold.
