---
import Analytics from "@vercel/analytics/astro"

const pathname = Astro.url.pathname.replace(/\/+$/, "") || "/"
const legal = pathname === "/policy" || pathname === "/terms" || pathname === "/data-deletion"
---

{legal ? null : <Analytics />}
